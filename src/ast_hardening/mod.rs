// use crate::ast;

// use crate::interpreter_soft::{SoftInt, SoftFloat, SoftBool};
// use crate::interpreter::{Value};

// pub fn harden_ast(
//     soft: ast::Program<SoftInt, SoftFloat, SoftBool, i64>,
// ) -> ast::Program<Value, Value, Value, i64> {
    

    
// }
